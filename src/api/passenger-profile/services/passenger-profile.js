'use strict';

/**
 * passenger-profile service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::passenger-profile.passenger-profile');
