'use strict';

/**
 * passenger-demand-observation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::passenger-demand-observation.passenger-demand-observation');
