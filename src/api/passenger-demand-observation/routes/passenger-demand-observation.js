'use strict';

/**
 * passenger-demand-observation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::passenger-demand-observation.passenger-demand-observation');
