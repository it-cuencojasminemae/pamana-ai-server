'use strict';

/**
 * passenger-demand-observation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::passenger-demand-observation.passenger-demand-observation');
